import { View, Text } from "react-native";

export default function Schedule() {
  return (
    <View className="flex-1 p-20 bg-background justify-center items-center">
      <Text className="text-4xl text-text font-bold">Settings</Text>
    </View>
  );
}

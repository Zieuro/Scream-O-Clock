// One spring config for every element so the whole screen shares the same feel.
export const BASE_SPRING_CONFIG = {
  mass: 1,
  stiffness: 240,
  damping: 20,
};

export type SlideItemProps = { index: number };

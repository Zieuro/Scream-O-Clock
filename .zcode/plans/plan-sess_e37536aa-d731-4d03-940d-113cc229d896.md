## Fix expo-doctor findings (Bun release-age blocked install)

1. **`bunfig.toml`**: append the 7 new exact versions to the existing `minimumReleaseAgeExcludes` list (same convention as the existing Sept entries): `expo@57.0.20`, `@expo/ui@57.0.16`, `expo-router@57.0.19`, `expo-build-properties@57.0.17`, `expo-image-picker@57.0.16`, `@react-native-async-storage/async-storage@2.2.0`, `react-native-svg@15.15.4`.
2. **`bunx expo install --check`**: rerun to perform the installs. If Bun blocks additional *transitive* `@expo/*` versions (like `@expo/cli` last time), add exactly those to the excludes and retry until clean.
3. **notifee advisory**: add `"expo": { "doctor": { "reactNativeDirectoryCheck": { "exclude": ["@notifee/react-native"] } } }` to `package.json` — the doctor-suggested way to record "known and accepted." Not switching notification libraries; notifee's exact-alarm support is load-bearing here.
4. **Verify**: `bunx expo-doctor` (expect 20/21 or all-pass), `bunx tsc --noEmit` + `bunx expo lint` (guards the async-storage 3→2 downgrade), and confirm the app boots.

Note: these are native-module version bumps, so your next Android test build will recompile them — no prebuild/config changes needed beyond the above.
